# Server configuration file

server.port=5000

# These two rooms prove the application supports at least two group chat rooms.
default.rooms=general,study

max.clients=50

log.file=logs/chat-server.log